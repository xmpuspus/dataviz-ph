# The vendor bundle is generated by `npm --prefix vendor/echarts-build run build`.
# Marking it generated keeps its minified text out of review diffs and out of the
# whitespace check. `git diff --exit-code` still reports a content change, so the
# reproducibility gate in .github/workflows/security.yml keeps working.
public/vendor/*.min.js -diff linguist-generated=true
